# The Range Automotive — website

Static single-page site. Deploy: drag this folder into Vercel/Netlify, or `vercel --prod`.

- `index.html` — the site
- `assets/` — badge (exact, do not remake), hero, photos
- Backend TODO: booking form endpoint, review automation, missed-call text-back (Mint & Co stack)
